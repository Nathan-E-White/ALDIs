// import org.openqa.selenium.WebDriver
import org.openqa.selenium.chrome.ChromeDriver
import kotlin.concurrent.timer


fun pauseBrowser(drv: ChromeDriver, maxTime: Number): Unit {}
fun moveMouseWheel(drv: ChromeDriver, maxSteps: Number = 1024): Unit {}
fun mouseDrift(drv: ChromeDriver, maxSteps: Number = 1024): Unit {}
fun 

fun main(args: Array<String>) {

    val driver = ChromeDriver()
    driver.get("https://selenium.dev")
    if (driver.errorHandler == selenium.error)
    driver.quit()
}
